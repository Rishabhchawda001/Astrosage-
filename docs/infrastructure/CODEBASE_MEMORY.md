# Codebase Memory MCP

Indexes the entire repository:
- Python files (classes, functions, imports)
- Markdown documentation
- JSON/YAML configuration
- Architecture docs, ADRs

Builds symbol index for semantic search.
Currently indexes 323+ Python files.
